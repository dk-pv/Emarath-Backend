/**
 * Prisma seed script (FND-01.2).
 *
 * Placeholder only: it wires up and proves the `prisma db seed` command that
 * FND-01.2 requires, but seeds NO data. Reference data, users, roles and any
 * domain records belong to later, separately-approved backlog tasks.
 */
function main(): void {
  console.log(
    '[seed] FND-01.2: no seed data to apply ' +
      '(reference/domain data is out of scope for this task).',
  );
}

main();
